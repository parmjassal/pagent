# Example environment variables for agent-platform configuration

# Enable or disable the guardrails for tool execution.
# When disabled, all tool calls will be permitted without validation.
# Default: false
# AGENT_PLATFORM_GUARDRAILS_ENABLED=true
